// Postgres connection + typed query helpers live here.
// Shanay wires this to Render Postgres via `pg` and DATABASE_URL.
// Intentionally empty during scaffold — frontend reads via src/lib/api.ts.

export {};
